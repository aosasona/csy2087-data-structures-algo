#include <vector>

using std::vector;

int alternating_sum(vector<int>);

vector<int> remove_duplicates(vector<int>);

bool compare(vector<int>, vector<int>);

void dump(vector<int>);
void dump(std::string, vector<int>);
